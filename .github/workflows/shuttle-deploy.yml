name: shuttle-deploy
on: 
  push:
    branches: [ "add-github-action" ]
  pull_request:
    branches: [ "add-github-action" ]

jobs:
  deploy-project:
    runs-on: ubuntu-latest
    steps:
      - name: Custom shuttle deploy action
        uses: denytwice/shuttle-deploy@v0.2-alpha
